<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:padding="8dp">

    <ImageView
        android:id="@+id/follow_user_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:contentDescription="@string/user_post_image"
        android:src="@drawable/amxs"
        android:scaleType="fitCenter"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <TextView
            android:id="@+id/follow_user_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?colorControlNormal"
            tools:text="Drew Stephens"
            android:fontFamily="@font/alata"
            android:textSize="@dimen/medium_text"/>

        <TextView
            android:id="@+id/follow_user_followers"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?colorControlNormal"
            tools:text="317 followers"
            android:fontFamily="@font/allerta"
            android:textSize="@dimen/small_text"/>
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/button_follow"
        android:layout_width="100dp"
        android:layout_height="40dp"
        android:backgroundTint="?colorTertiaryFixedDim"
        android:textSize="14sp"
        android:text="@string/action_follow"
        android:layout_marginEnd="10dp"
        android:textColor="@color/black"/>
</LinearLayout>